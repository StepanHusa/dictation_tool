Make a working linux app/tool that transcribes speach into text. I want to use it to dictate (primarily english) text into terminal, vim, vscode, ideally anywhere where my coursor is and where i can type by keyboard. I want it to correctly switch the device (e.g. my headphones have mode where they have nice playback and a mode where they have the mic on). Think about every option. 
I want to bind it to a key shortcut (that runs a command) but it can start on the first use and keep running (to be ready). I imagine it will make a popup (like linuxmint notifications) that says it started listening and i press a different shortcut to stop it. Or possibely any keystroke stops it. Search if there is something similar in the web we could customize and make a todo list as well as testing scenarios. 
Make a ralphloop prompt using the template in the directory. Look into the ~/Source/coding_preferences to see what i mean. put it in a new file here.
